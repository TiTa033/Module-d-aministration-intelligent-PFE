databaseChangeLog:
  - include:
      file: db/changelog/changes/001-baseline.yaml
  - include:
      file: db/changelog/changes/004-seed-demo-data.yaml
  - include:
      file: db/changelog/changes/005-seed-demo-data-rerun.yaml
  - include:
      file: db/changelog/changes/006-fix-demo-password-hash.yaml
  - include:
      file: db/changelog/changes/007-force-demo-password-hash.yaml
  # NOTE:
  # The "add/drop phone" changesets were only a Liquibase test.
  # After recreating the database from scratch, there is no baseline schema yet,
  # so trying to ALTER TABLE users would fail (users table doesn't exist).
  # We'll reintroduce these changesets once the full schema is managed by Liquibase.
